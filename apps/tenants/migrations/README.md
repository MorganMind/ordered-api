# Migrations Directory

This directory contains Django database migrations for the `tenants` app.

## Next Steps

After installing dependencies (see main README), run:

```bash
python manage.py makemigrations tenants
python manage.py migrate
```

This will create the initial migration files for the Tenant model.

